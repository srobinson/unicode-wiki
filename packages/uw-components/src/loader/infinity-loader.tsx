import * as React from "react"
import * as Styled from "./infinity-loader.css"

export class InfinityLoader extends React.PureComponent {
  render() {
    return (
      <Styled.Container>
        <Styled.InfinityLoader />
      </Styled.Container>
    )
  }
}
