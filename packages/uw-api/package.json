{
  "name": "@uw/api",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "prepare": "echo '@uw/api' && tsc",
    "dev": "ts-node-dev src/index.ts",
    "lint": "tslint 'src/**/*.ts'",
    "start:api": "node dist/index.js",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@uw/domain": "^0.1.0",
    "@uw/logging": "^0.1.0",
    "@uw/utils": "^0.1.0"
  },
  "devDependencies": {
    "@types/tmp": "0.0.33"
  }
}
