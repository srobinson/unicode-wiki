version: "3"
services:
  mongo:
    command: mongod
    container_name: uw-mongo
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - $HOME/_uw_data:/data/db
      - mongodb_config:/data/configdb
volumes:
  mongodb_config:
    driver: local
networks:
  elk:
